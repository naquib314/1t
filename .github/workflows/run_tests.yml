name: Build app

on: push

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build depmap-base docker image
        run: docker build -t depmap-base .
